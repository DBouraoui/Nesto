meta {
  name: Forget password
  type: http
  seq: 4
}

post {
  url: http://localhost:8000/api/v1/auth/forget-password
  body: json
  auth: inherit
}

body:json {
  {
    "email":"{{email}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
